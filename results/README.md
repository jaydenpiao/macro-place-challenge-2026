# Results

Generated experiment outputs live under `results/<run-id>/` and are ignored by Git.

Tracked files in this directory:

- `README.md`: operating notes
- `summary.schema.json`: machine-readable result contract

Each run should produce at least:

- `summary.json`
- optional `run.log`
- optional visualization or ORFS artifacts for finalist candidates
